# Firebase credentials are automatically provided by Firebase Hosting
# via /__/firebase/init.json - no need to set them here for production

# Only set VITE_USE_EMULATORS=true when running locally with emulators
# Comment out for production builds:
# VITE_USE_EMULATORS=true
