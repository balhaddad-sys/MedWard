# =============================================================================
# MedWard Pro - Firebase Configuration
# =============================================================================
# Copy this file to .env:  cp .env.example .env
#
# For LOCAL DEVELOPMENT with emulators, use the demo values below as-is.
# For PRODUCTION, replace with your real Firebase project credentials from:
#   https://console.firebase.google.com → Project Settings → Your apps
# =============================================================================

# --- Local development (emulator-ready defaults) ---
VITE_FIREBASE_API_KEY=demo-medward-api-key
VITE_FIREBASE_AUTH_DOMAIN=demo-medward-pro.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=demo-medward-pro
VITE_FIREBASE_STORAGE_BUCKET=demo-medward-pro.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:demo

# Set to 'true' to connect to Firebase emulators in development.
# When using the demo values above, this MUST be 'true'.
VITE_USE_EMULATORS=true

# Release stage:
# - pilot: enables supervised-pilot messaging and relaxed local checks
# - production: enforces strict runtime safety checks (e.g., App Check key required)
VITE_RELEASE_STAGE=pilot

# --- App Check (PRODUCTION ONLY - prevents Cloud Function abuse) ---
# Get your ReCaptcha v3 site key from: https://www.google.com/recaptcha/admin
# Register your production domains (e.g., yourapp.web.app, yourapp.com)
# Leave empty for development (App Check will be disabled when using emulators)
VITE_RECAPTCHA_SITE_KEY=

# AI model is configured server-side in Cloud Functions (ANTHROPIC_API_KEY stored as Firebase secret)
